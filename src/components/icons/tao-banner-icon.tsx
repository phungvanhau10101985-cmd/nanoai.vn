'use client'

import { ToolIconImage } from './tool-icon-image'

export function TaoBannerIcon({ className }: { className?: string; strokeWidth?: number }) {
  return <ToolIconImage src="/tool-icons/tao-banner.webp" className={className} />
}