'use client'

import { ToolIconImage } from './tool-icon-image'

export function LamDepAnhIcon({ className }: { className?: string; strokeWidth?: number }) {
  return <ToolIconImage src="/tool-icons/lam-dep-anh.webp" className={className} />
}
