'use client'

import { ToolIconImage } from './tool-icon-image'

export function GhepAnhIcon({ className }: { className?: string; strokeWidth?: number }) {
  return <ToolIconImage src="/tool-icons/ghep-anh.webp" className={className} />
}