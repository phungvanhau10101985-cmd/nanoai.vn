'use client'

import { ToolIconImage } from './tool-icon-image'

export function HoanDoiKhuonMatIcon({ className }: { className?: string; strokeWidth?: number }) {
  return <ToolIconImage src="/tool-icons/hoan-doi-khuon-mat.webp" className={className} />
}