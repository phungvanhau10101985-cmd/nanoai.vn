'use client'

import { ToolIconImage } from './tool-icon-image'

export function ThayNenSanPhamIcon({ className }: { className?: string; strokeWidth?: number }) {
  return <ToolIconImage src="/tool-icons/thay-nen-san-pham.webp" className={className} />
}