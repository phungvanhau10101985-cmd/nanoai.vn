'use client'

import { ToolIconImage } from './tool-icon-image'

export function ThietKeLogoIcon({ className }: { className?: string; strokeWidth?: number }) {
  return <ToolIconImage src="/tool-icons/thiet-ke-logo.webp" className={className} />
}
