'use client'

import { ToolIconImage } from './tool-icon-image'

export function XoaVatTheIcon({ className }: { className?: string; strokeWidth?: number }) {
  return <ToolIconImage src="/tool-icons/xoa-vat-the.webp" className={className} />
}