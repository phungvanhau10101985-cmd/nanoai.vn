'use client'

import { ToolIconImage } from './tool-icon-image'

export function LamNetAnhIcon({ className }: { className?: string; strokeWidth?: number }) {
  return <ToolIconImage src="/tool-icons/lam-net-anh.webp" className={className} />
}
