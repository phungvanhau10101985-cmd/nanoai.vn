'use client'

import { ToolIconImage } from './tool-icon-image'

export function CheAnhIcon({ className }: { className?: string; strokeWidth?: number }) {
  return <ToolIconImage src="/tool-icons/che-anh.webp" className={className} />
}
